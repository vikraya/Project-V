import 'package:flutter/material.dart';

class AppColors {
  static const Color scaffoldBackground =
      Color(0xff000000); //sacffold background color

  // bottom navigation bar colors

  static const Color bottomNavigationBarColorMS =
      Color(0xff5271ff); //bottom navigation bar color of MS
  static const Color bottomNavigationBarColorf =
      Color(0xff5ce1e6); //bottom navigation bar color of F
  static const Color bottomNavigationBarColorLe =
      Color(0xff4ce305); //bottom navigation bar color of LE
  static const Color bottomNavigationBarColorLg =
      Color(0xffe3a705); //bottom navigation bar color of LG
  static const Color bottomNavigationBarColorC =
      Color(0xff737373); //bottom navigation bar color of P

  // App bar colors

  static const Color transparent =
      Colors.transparent; //bottom navigation bar color
  static const LinearGradient AppBarColorMS = LinearGradient(
      colors: [Color(0xff3533cd), Color(0xff000000)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter); //app bar color OF MS
  static const LinearGradient AppBarColorF = LinearGradient(
      colors: [Color(0xff5ce1e6), Color(0xff000000)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter); //app bar color OF F
  static const LinearGradient AppBarColorLe = LinearGradient(
      colors: [Color(0xff4ce305), Color(0xff000000)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter); //app bar color OF LE
  static const LinearGradient AppBarColorLg = LinearGradient(
      colors: [Color(0xffe3a705), Color(0xff000000)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter); //app bar color OF LG
  static const LinearGradient AppBarColorC = LinearGradient(
      colors: [Color(0xff737373), Color(0xff000000)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter); //app bar color OF P
}


